# 第7段階：端末別統合確認・ZIP納品 検証メモ

## 対象

- タイマー画面
- 経路編集ページ
- My設定ページ
- 設定ページ
- 横線路・縦線路・円形線路
- 運行中の電車リスト
- すぐ使う
- 新しい電車を追加
- 複数電車の同時カウント
- 到着通知
- いたずら防止ロック
- 画面をつけたままにする設定
- localStorage保存
- PWAキャッシュ

## 実施した統合調整

- アプリバージョンを `1.17.5` に更新しました。
- Service Workerキャッシュ名を `train-timer-v1.17.5` に更新しました。
- iPad・タブレット縦画面で、線路カードと操作カードが圧縮されて近づきすぎないように、タイマー画面を自然な縦積みへ補正しました。
- iPad・タブレット縦画面で、固定下部ナビと操作ボタンが重ならないように、線路表示エリアの高さと下部余白を補正しました。
- タイマー画面は1本表示を維持し、運行中の電車リストはMy設定ページへ整理した状態を維持しました。
- 操作ボタン、カード、ナビはGrid/Flex中心の配置を維持しました。

## 端末別UI確認

ChromiumへHTML/CSS/JavaScriptを直接展開し、localStorage相当の保存領域をモックして確認しました。
通常の `file://` / `localhost` 表示はこの実行環境の管理ポリシーで遮断されるため、実機ブラウザ確認は別途必要です。

| 端末相当 | 画面サイズ | 横スクロール | 下部ナビと操作の重なり | 線路と操作の重なり | 主操作ボタン高 | JSエラー |
|---|---:|---|---|---|---:|---:|
| iPhone相当 | 390×844 | なし | なし | なし | 48px | 0 |
| Android相当 | 412×915 | なし | なし | なし | 48px | 0 |
| iPad縦 | 768×1024 | なし | なし | なし | 54px | 0 |
| タブレット横 | 1024×800 | なし | なし | なし | 52px | 0 |
| PC | 1366×900 | なし | なし | なし | 52px | 0 |

## 画面別確認

- タイマー画面：横線路・縦線路・円形線路を確認しました。
- 経路編集ページ：現在設定、駅一覧、区間設定、線路の形、変更反映、取消の表示を確認しました。
- My設定ページ：すぐ使う、運行中の電車、登録した経路、データ管理の表示を確認しました。
- 設定ページ：音、表示、文字サイズ、高コントラスト、動きを減らす、画面ロック、画面維持の説明を確認しました。
- タイマー画面には、運行中の電車リスト本体は表示されず、小さな本数表示だけが残っています。
- 指定された除外路線名は、アプリ本体ファイルに含まれていません。

## 機能確認

| 項目 | 結果 |
|---|---|
| 既存の1本表示タイマー | `30:00` から `28:30` へ進むことを確認 |
| 一時停止 | 運転中に一時停止ボタンが表示されることを確認 |
| つづきから・はじめから | 一時停止後に両方が表示されることを確認 |
| すぐ使う | 5路線が表示されることを確認 |
| 複数電車追加 | すぐ使うから追加し、3カード表示を確認 |
| 複数電車同時カウント | 2本が同時に運転中になることを確認 |
| 表示中でない電車の進行 | 運行中リストの時刻が進むことを確認 |
| 新しい電車を追加 | 1分の確認用電車を作成し、タイマー画面へ表示できることを確認 |
| 到着通知 | 到着後に完了パネルまたは到着表示が出ることを確認 |
| 音の重なり対策 | ゴール音は短時間連続再生を抑制する実装が維持されていることを確認 |
| いたずら防止ロック | ロック表示が出ることを確認 |
| ロック中の誤操作防止 | ロック中に開始操作が通らないことを確認 |
| ロック解除 | 3秒長押しで解除されることを確認 |
| localStorage保存 | 保存データに `trains` が含まれることを確認 |
| 再読み込み復元 | 保存済みデータを再投入して運行中本数が復元されることを確認 |
| 旧保存データ | 古い `schemaVersion` の保存データでもページが壊れないことを確認 |
| Wake Lock非対応 | 非対応分岐があり、エラーで止まらないことを確認 |
| PWAキャッシュ | `train-timer-v1.17.5` へ更新済み |

## 静的確認

- `node --check app.js`：成功
- `node --check sw.js`：成功
- `manifest.webmanifest`：JSON形式正常
- HTMLのID重複：なし
- JavaScriptから参照しているIDの欠落：なし
- ARIA参照切れ：なし
- CSS波括弧数：一致
- ZIP破損：なし
- APIキー、秘密鍵、認証情報：なし

## 未確認・実機で追加確認が必要な項目

- iPhone Safariのアドレスバー伸縮時の見え方
- Android Chromeのジェスチャーナビ／3ボタンナビ差
- iPad Split Viewの細かい幅
- 実機でのPWAインストール後のService Worker更新
- 実機でのオフライン再起動
- 実機でのWake Lock作動
- OS文字サイズ最大時の最終表示

## GitHubへの反映

GitHubへのcommit、push、ブランチ作成、Pull Request作成、GitHub上の直接編集、Vercelなどへのデプロイは行っていません。
